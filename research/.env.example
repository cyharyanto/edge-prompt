# EdgePrompt Research Environment Configuration Example
# Copy this file to .env and update with your values

# LM Studio API URL (required for using LM Studio)
# This is the URL where the LM Studio server is running
LM_STUDIO_URL=http://localhost:1234

# Anthropic API Key (required for evaluation_with_llm_proxy)
# Get your API key from: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-api03-...

# Optional: OpenAI API Key for alternative evaluation proxy
# OPENAI_API_KEY=sk-...

# Optional: Mock Mode configuration (overrides command-line flag)
# Set to "true" to use mock models instead of real LLMs
# MOCK_MODE=false 